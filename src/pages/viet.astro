---
import Layout from '../layouts/BlogPost.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('viet')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<Layout
	title="Vietpost"
	description="viet posts"
	pubDate={new Date('Oct 16 2025')}
	updatedDate={new Date('Oct 16 2025')}
>
	Under construction
</Layout>